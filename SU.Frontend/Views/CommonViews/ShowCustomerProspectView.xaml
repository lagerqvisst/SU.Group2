<Window x:Class="SU.Frontend.Views.CommonViews.ShowCustomerProspectView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:helper="clr-namespace:SU.Frontend.Helper.SU.Frontend.Helper"
        xmlns:controls="clr-namespace:SU.Frontend.Views.UserControls"
        Title="ShowCustomerProspectView" 
        Width="1000" Height="600"
        WindowStartupLocation="CenterScreen">


    <Window.Resources>
        <helper:ViewModelLocator x:Key="Locator" />
    </Window.Resources>

    <Grid DataContext="{Binding ShowCustomerProspectViewModel, Source={StaticResource Locator}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Prospects" FontSize="24" FontWeight="Bold" Grid.Row="0" Margin="10"/>
        <!-- Date Range Selection -->
        <StackPanel Orientation="Horizontal" Margin="10" Grid.Row="1">




        </StackPanel>


        <DataGrid ItemsSource="{Binding Prospects}" SelectedItem="{Binding SelectedProspect}" AutoGenerateColumns="True" Grid.Row="2" Margin="10,28,10,146"/>

        <Button Content="Save changes" Command="{Binding SaveProspectCommand}" Grid.Row="3" Margin="720,17,160,26" Height="60" Width="120"/>

        <StackPanel VerticalAlignment="Bottom" Grid.Row="3" Height="100" RenderTransformOrigin="0.501,0.475">



            <controls:DownloadButton Command="{Binding ExportComissionExcel}" 
                 HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10"
                 Grid.Row="3"/>

        </StackPanel>


    </Grid>
</Window>
