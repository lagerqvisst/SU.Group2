<Window x:Class="SU.Frontend.Views.CommonViews.ShowCustomerProspectView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:helper="clr-namespace:SU.Frontend.Helper.SU.Frontend.Helper"
        xmlns:controls="clr-namespace:SU.Frontend.Views.UserControls"
        Title="ShowCustomerProspectView" 
        Width="1000" Height="600"
        WindowStartupLocation="CenterScreen">


    <Window.Resources>
        <helper:ViewModelLocator x:Key="Locator" />
    </Window.Resources>

    <Grid DataContext="{Binding ShowCustomerProspectViewModel, Source={StaticResource Locator}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Prospects" FontSize="24" FontWeight="Bold" Grid.Row="0" Margin="10"/>

        <TextBlock Text="Note" FontWeight="Bold" Grid.Row="2" Margin="60,0,0,0"/>

        <DataGrid ItemsSource="{Binding Prospects}" SelectedItem="{Binding SelectedProspect}" AutoGenerateColumns="True" Grid.Row="1" Margin="10,28,10,56"/>


        <Border BorderBrush="Green" BorderThickness="2" Grid.Row="3" Margin="0,0,0,0">
            <StackPanel VerticalAlignment="Top" Grid.Row="3" Height="102" Margin="0,-2,0,0" Orientation="Horizontal">
                <!--Used for Note-->
                <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                    <TextBox Text="{Binding SelectedProspect.ContactNote, Mode=TwoWay}" Width="161" Height="93" TextWrapping="Wrap" AcceptsReturn="True"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Button Content="Save changes" Command="{Binding SaveProspectCommand}" Grid.Row="4" Margin="736,33,144,10" Height="60" Width="120"/>

        <controls:DownloadButton Command="{Binding ExportComissionExcel}" 
                 HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10"
                 Grid.Row="4"/>

    </Grid>
</Window>
