<Window x:Class="SU.Frontend.Views.CommonViews.ShowCustomerProspectView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:helper="clr-namespace:SU.Frontend.Helper.SU.Frontend.Helper"
        Title="ShowCustomerProspectView" 
        Width="1000" Height="600"
        WindowStartupLocation="CenterScreen">


    <Window.Resources>
        <helper:ViewModelLocator x:Key="Locator" />
    </Window.Resources>

    <Grid DataContext="{Binding ShowCustomerProspectViewModel, Source={StaticResource Locator}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Prospects" FontSize="24" FontWeight="Bold" Grid.Row="0" Margin="10"/>
        <!-- Date Range Selection -->
        <StackPanel Orientation="Horizontal" Margin="10" Grid.Row="1">
            
            
            <TextBlock Text="Date of contact: " VerticalAlignment="Center" Margin="10,0,0,0"/>
            <DatePicker SelectedDate="{Binding EndDate, Mode=TwoWay}" Margin="5,0"/>
            <Button Content="Update prospects" Command="{Binding GenerateCommissionCommand}" Margin="10,0"/>
        </StackPanel>

        <!-- DataGrid to Display Commissions -->
        <DataGrid ItemsSource="{Binding Commissions}" AutoGenerateColumns="True" Grid.Row="2" Margin="10"/>
        
    </Grid>
</Window>
